# Uncomment block below for debug/testing of raw (unformatted) events
#log {
#    source(s_logzilla);
#    destination(df_raw);
#};

log {
    source(s_logzilla);
    # disable s_src if you don't want local server events
    source(s_src);
    rewrite(rw_cisco_ise);
    destination(d_logzilla);
    # Uncomment line below for debug/testing of incoming events
    # destination(df_debug);

    flags(flow-control);
};
