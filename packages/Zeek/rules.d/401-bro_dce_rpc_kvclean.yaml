rewrite_rules:
- comment:
  - Remove kv pairs that have no values 
  - 'e.g: foo="-"'
  match:
    field: program
    op: =*
    value: bro_dce_rpc
  replace:
    field: message
    expr: \S+="-"
    fmt: ""
